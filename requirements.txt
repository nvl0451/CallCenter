fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0
openai>=1.51.0
chromadb>=0.5.5
numpy<2,>=1.26.4
# Optional deps (install manually if needed):
# sentence-transformers>=3.0   # requires torch
# torch>=2.2                   # install per PyTorch instructions
# torchvision>=0.17
# open-clip-torch>=2.24        # vision (requires torch)
Pillow>=10.3                   # vision image handling
python-multipart>=0.0.9        # file upload
python-telegram-bot>=20.3      # telegram bot (optional)
